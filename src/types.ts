// Re-export from new domain types for backward compatibility
export type { Lead, LeadStatus, LeadSource, UserRole, ActivityAction, TaskPriority, Activity, Task, AppUser, SavedFilter, FilterState } from './types/domain';
